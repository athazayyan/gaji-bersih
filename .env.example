# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Get these values from: https://supabase.com/dashboard/project/<your-project>/settings/api

# Supabase Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Anon/Public Key (safe to expose to client)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (NEVER expose to client, server-side only)
# Used for admin operations like GC cleanup
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =============================================================================
# OPENAI CONFIGURATION
# =============================================================================
# Get your API key from: https://platform.openai.com/api-keys

# OpenAI API Key
OPENAI_API_KEY=sk-your-openai-api-key-here

# OpenAI Vector Store IDs
# Create these vector stores in OpenAI Dashboard: https://platform.openai.com/storage
# Global Store: For government regulations (admin-managed, read-only)
OPENAI_GLOBAL_VECTOR_STORE_ID=vs_xxx_global

# Big Store: For all user documents (ephemeral + persistent) with metadata filtering
OPENAI_BIG_VECTOR_STORE_ID=vs_xxx_big_store

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Application Base URL (used for redirects, webhooks)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Environment (development, staging, production)
NODE_ENV=development

# Session TTL in minutes (default: 90 minutes)
SESSION_TTL_MINUTES=90

# Maximum file upload size in MB (default: 10MB)
MAX_FILE_SIZE_MB=10

# Allowed file MIME types (comma-separated)
ALLOWED_MIME_TYPES=application/pdf,image/jpeg,image/png,image/webp,text/plain

# =============================================================================
# OPTIONAL: OBSERVABILITY & MONITORING
# =============================================================================

# Sentry DSN (for error tracking)
# NEXT_PUBLIC_SENTRY_DSN=

# Analytics (PostHog, Mixpanel, etc.)
# NEXT_PUBLIC_POSTHOG_KEY=
# NEXT_PUBLIC_POSTHOG_HOST=

# =============================================================================
# DEVELOPMENT ONLY
# =============================================================================

# Enable debug logging in development
DEBUG=false

# Skip auth checks in development (NEVER use in production!)
# SKIP_AUTH_DEV=false
